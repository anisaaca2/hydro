<?php
// class Rating {
//     private $db;

//     public function __construct($db) {
//         $this->db = $db;
//     }

//     public function addRating($productId, $userId, $rating) {
//         $stmt = $this->db->prepare("INSERT INTO ratings (product_id, user_id, rating) VALUES (?, ?, ?)");
//         return $stmt->execute([$productId, $userId, $rating]);
//     }

//     public function getAverageRating($productId) {
//         $stmt = $this->db->prepare("SELECT AVG(rating) as average FROM ratings WHERE product_id = ?");
//         $stmt->execute([$productId]);
//         return $stmt->fetchColumn();
//     }
// }
?>

<!-- KODE INI SEMENTARA DIBISUKAN -->